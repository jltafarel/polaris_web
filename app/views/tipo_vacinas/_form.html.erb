<%= form_for(@tipo_vacina) do |f| %>
  <% if @tipo_vacina.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_vacina.errors.count, "error") %> prohibited this tipo_vacina from being saved:</h2>

      <ul>
      <% @tipo_vacina.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_vacina %><br>
    <%= f.text_field :tipo_vacina %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
