<%= form_for(@classificacao) do |f| %>
  <% if @classificacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classificacao.errors.count, "error") %> prohibited this classificacao from being saved:</h2>

      <ul>
      <% @classificacao.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Classificação: " %><br>
    <%= f.text_field :classificao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
