<%= form_for(@nascimento) do |f| %>
  <% if @nascimento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nascimento.errors.count, "error") %> prohibited this nascimento from being saved:</h2>

      <ul>
      <% @nascimento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br>
    <%= f.date_select :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
