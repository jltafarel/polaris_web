<%= form_for(@medicamento) do |f| %>
  <% if @medicamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medicamento.errors.count, "error") %> prohibited this medicamento from being saved:</h2>

      <ul>
      <% @medicamento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<table>
		<tr>
			<td>
				<%= f.label :nome %>
			</td>
			<td>
				<%= f.text_field :nome %>
			</td>
		</tr>
		<tr>
			<td>
				<%= f.label :tipo_medicamento_id %>
			</td>
			<td>
				<%= f.select :tipo_medicamento_id, TipoMedicamento.find(:all).collect {|item| [item.tipo_medicamento, item.id]} %>
			</td>		
  		</tr>
  		<tr>
  			<td colspan="2">
  				<%= f.submit %>
  			</td>
  		</tr>
  </table>			
<% end %>
