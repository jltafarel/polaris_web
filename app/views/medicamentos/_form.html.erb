<%= form_for(@medicamento) do |f| %>
  <% if @medicamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medicamento.errors.count, "error") %> prohibited this medicamento from being saved:</h2>

      <ul>
      <% @medicamento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	
	<div class="field">
		<%= f.label :nome %></br>
		<%= f.text_field :nome %>
	</div>
	<div class="field">
		<%= f.label :tipo_medicamento_id %></br>
		<%= f.collection_select :tipo_medicamento_id, TipoMedicamento.all, :id, :tipo_medicamento%>
		<%= link_to 'Novo tipo medicamento', new_tipo_medicamento_path %>		
  	</div>
  	<div class="actions">
  		<%= f.submit %>
  	</div>			
<% end %>
