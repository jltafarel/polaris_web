<%= form_for(@permissao) do |f| %>
  <% if @permissao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@permissao.errors.count, "error") %> prohibited this permissao from being saved:</h2>

      <ul>
      <% @permissao.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :permissao %><br>
    <%= f.text_field :permissao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
