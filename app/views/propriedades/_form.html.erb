<%= form_for(@propriedade) do |f| %>
  <% if @propriedade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@propriedade.errors.count, "error") %> prohibited this propriedade from being saved:</h2>

      <ul>
      <% @propriedade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
