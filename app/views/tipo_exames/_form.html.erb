<%= form_for(@tipo_exame) do |f| %>
  <% if @tipo_exame.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_exame.errors.count, "error") %> prohibited this tipo_exame from being saved:</h2>

      <ul>
      <% @tipo_exame.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_exame %><br>
    <%= f.text_field :tipo_exame %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
